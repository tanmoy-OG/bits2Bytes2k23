export const UserProfile = ()=>{

    return(
        <div className="">
            
        </div>
    );
}

// Parti:
// Roll (Edit)
// Name (Edit)
// Email (Edit)

// Event (Left) 
// Team (Right)